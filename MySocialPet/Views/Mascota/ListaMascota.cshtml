@model IEnumerable<MySocialPet.Models.ViewModel.Mascotas.ListaMascotaViewModel>
@{
    ViewData["Title"] = "Mis Mascotas";
}

<div class="container mb-5">
    <h2>Animales</h2>

    <div class="row row-cols-1 row-cols-md-3 g-4 mt-3">
        @foreach (var mascota in Model)
        {
            <div class="col">
                <div class="card h-100">
                    @if (mascota.Foto != null && mascota.Foto.Length > 0)
                    {
                        var base64Image = "data:image/jpeg;base64," + Convert.ToBase64String(mascota.Foto);
                        <img src="@base64Image" class="card-img-top" alt="Foto de @mascota.Nombre" />
                    }
                    else
                    {
                        <img src="foto.jpg" class="card-img-top" alt="Sin foto" />
                    }

                    <div class="card-body">
                        <h5 class="card-title">@mascota.Nombre</h5>
                        <p class="card-text">
                            <strong>Género:</strong> @mascota.Genero<br />
                            <strong>Nacimiento:</strong> @(mascota.Nacimiento?.ToString("dd/MM/yyyy") ?? "N/A")<br />
                            <strong>Peso:</strong> @(mascota.PesoKg?.ToString("0.00") ?? "N/A") kg<br />
                            <strong>Longitud:</strong> @(mascota.LongitudCm?.ToString("0.00") ?? "N/A") cm<br />
                            <strong>BCS:</strong> @(mascota.BCS.ToString() ?? "N/A")<br />
                            <strong>Esterilizado:</strong> @(mascota.Esterilizada ? "Sí" : "No")
                        </p>
                        <a href="@Url.Action("Details", "Mascota", new { id = mascota.Id })" class="btn btn-primary">Ver detalles</a>
                    </div>
                </div>
            </div>
        }

        <div class="col">
            <div class="card h-100 text-center shadow-sm" style="min-height: 300px;">
                <a href="@Url.Action("CreateMascota", "Mascota")" class="text-decoration-none text-dark h-100 w-100">
                    <div class="card-body d-flex flex-column justify-content-center align-items-center h-100">
                        <div class="d-flex justify-content-center align-items-center mb-3" style="width: 100px; height: 100px;">
                            <span style="font-size: 3rem; font-weight: bold;">+</span>
                        </div>
                        <p class="card-text fw-semibold">Añadir nuevo mascota</p>
                    </div>
                </a>
            </div>
        </div>
    </div>
</div>

<style>
    .card:hover {
        box-shadow: 0 0 20px rgba(255, 255, 255, 0.6), 0 0 40px rgba(255, 215, 0, 0.5);
        transition: box-shadow 0.3s ease-in-out;
        border-color: blue;
    }
</style>