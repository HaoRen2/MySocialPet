
@model MySocialPet.Models.ViewModel.Foros.IndexForoViewModel;

@{
    ViewData["Title"] = "Foro - comunidad";
}

<div class=" pt-nav test">

    <div class="container-fluid w-75 mt-5">
        <div class="row">
            <div class="col-8">
                <div class="bg-light shadow rounded-3 p-4">
                    <h1 class="mb-5">Zona categorías</h1>
                    @{
                        foreach (var item in Model.Foros)
                        {
                            <div class="d-flex align-items-center justify-content-between pe-2">
                                <div class="d-flex align-items-center">
                                    <div>
                                        <img src="@Url.Content("~/src/foro/icons/" + @item.Icono)" width="50px" class="img-fluid mx-4 mb-3" alt="Imagen foro" />
                                    </div>
                                    <div>
                                        <h2 class="fs-5 fw-bold">@item.Nombre</h2>
                                        <p>@item.Descripcion</p>
                                    </div>
                                </div>
                                <span class="btn btn-verde fs-6 py-1">Ir</span>
                            </div>
                            <hr />
                        }
                    }


                </div>
            </div>
            <div class="col-4">
                <div class="bg-light shadow rounded p-4">
                    <img style="background-image: var(--trending_icon); height: 28px; width: 28px; margin-right: 4px" src="@Url.Content("~/src/foro/icons/Fire.svg")" class="d-inline mb-3" alt="Persona enviando mensaje desde el móvil" />
                    <h3 class="d-inline">Trending</h3>
                    <p style="color: #a4a4a4">Los hilos del momento en MySocialPet</p>
                </div>
            </div>
        </div>

    </div>
</div>


