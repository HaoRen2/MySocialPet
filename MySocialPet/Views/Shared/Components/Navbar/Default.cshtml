<nav class="navbar navbar-expand-lg px-3 py-3 fixed-top navbar-main">
    <div class="container-fluid d-flex justify-content-between align-items-center">

        <!-- Logo -->
        <a class="navbar-brand d-flex align-items-center text-white fw-bold fs-1 me-3"
           asp-controller="Home" asp-action="Index">
            MySocialPet 🐾
        </a>

        <!-- LOGIN + HAMBURGUESA en móvil -->
        <div class="d-flex align-items-center d-lg-none">
            <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav">
                <i class="bi bi-list menu-icon" id="toggleSidebar"></i>
            </button>
        </div>

        <!-- MENÚ -->
        <div class="collapse navbar-collapse justify-content-end" id="navbarNav">
            <ul class="navbar-nav me-1 align-items-center">
                @if (ViewBag.IsAuthenticated)
                {
                    <li class="nav-item">
                        <a class="nav-link text-white fw-semibold" href="@Url.Action("ListaMascota", "Mascota")">
                            <i class="bi bi-house-heart me-1"></i>Mis Mascotas
                        </a>
                    </li>
                    <li class="separador-vertical"></li>
                    <li class="nav-item dropdown">
                        <a class="nav-link dropdown-toggle d-flex align-items-center" href="#" role="button" data-bs-toggle="dropdown" aria-expanded="false">
                            Accion
                        </a>
                        <ul class="dropdown-menu dropdown-menu-end">
                            <li>
                                 <a class="dropdown-item" href="@Url.Action("SaludRegistro", "Salud", new { id = Model })">
                            <i class="bi bi-heart-pulse me-1"></i> Salud
                        </a>
                            </li>
                            <li>
                        <a class="dropdown-item" href="@Url.Action("Vacunas", "Salud", new { id = Model })">
                            <i class="bi bi-clipboard-plus me-1"></i> Vacuna
                        </a>
                            </li>
                            <li>
                        <a class="dropdown-item" href="@Url.Action("CalendarioEventos", "Salud", new { id = Model })">
                            <i class="bi bi-calendar me-1"></i> Evento
                        </a>
                            </li> 
                        </ul>
                    </li>

                    <li class="separador-vertical"></li>
                    <li class="nav-item nav-section">
                        <a class="nav-link text-white fw-semibold" href="@Url.Action("ListAlbum", "Albums")">
                            <i class="bi bi-images me-1"></i>Álbum
                        </a>
                    </li>
                    <li class="separador-vertical"></li>
                    <li class="nav-item nav-section">
                        <a class="nav-link text-white fw-semibold" href="@Url.Action("Index", "Sugerencia")">
                            <i class="bi bi-book me-1"></i>Sugerencia
                        </a>
                    </li>
                    <li class="separador-vertical"></li>
                    <li class="nav-item nav-section">
                        <a class="nav-link text-white fw-semibold" href="@Url.Action("Index", "Foro")">
                            <i class="bi bi-chat-dots me-1"></i>Foro
                        </a>
                    </li>
                    <li class="separador-vertical"></li>
                    <li class="nav-item dropdown">
                        <a class="nav-link dropdown-toggle d-flex align-items-center" href="#" role="button" data-bs-toggle="dropdown" aria-expanded="false">
                            <img src="~/src/DefaultAvatar.png"
                                 class="rounded-circle me-2"
                                 style="width:32px;height:32px;object-fit:cover;">
                            <span class="d-none d-sm-inline">@ViewBag.Username</span>
                        </a>
                        <ul class="dropdown-menu dropdown-menu-end">
                            <li>
                                <a class="dropdown-item d-flex align-items-center" asp-controller="Perfil" asp-action="Index">
                                    <i class="bi bi-person me-2"></i> Mi perfil
                                </a>
                            </li>
                            <li><hr class="dropdown-divider" /></li>
                            <li class="px-2">
                                <form asp-controller="Login" asp-action="Logout" method="post">
                                    @Html.AntiForgeryToken()
                                    <button type="submit" class="dropdown-item w-100 text-start p-0">
                                        <span class="d-flex align-items-center p-2">
                                            <i class="bi bi-box-arrow-right me-2"></i> Cerrar sesión
                                        </span>
                                    </button>
                                </form>
                            </li>
                        </ul>
                    </li>
                }
                else
                {
                    <li class="nav-item nav-section">
                        <a class="nav-link" href="#sobre">Sobre</a>
                    </li>
                    <li class="separador-vertical"></li>
                    <li class="nav-item nav-section">
                        <a class="nav-link" href="#caracteristicas">Características</a>
                    </li>
                    <li class="separador-vertical"></li>
                    <li class="nav-item nav-section">
                        <a class="nav-link" href="#comunidad">Comunidad</a>
                    </li>
                    <li class="separador-vertical"></li>
                    <li class="nav-item nav-section">
                        <a class="nav-link" href="#contacto">Contacto</a>
                    </li>
                    <li class="nav-item">
                        <a href="@Url.Action("Index", "Login")" class="btn animated fs-4 d-none d-lg-inline">&nbsp;Login&nbsp;</a>
                    </li>
                }
            </ul>
        </div>
    </div>
</nav>

